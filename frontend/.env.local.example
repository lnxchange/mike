# Server-only upstream used by the same-origin /api gateway.
API_BASE_URL=http://localhost:3001

# Optional branding/config profile from frontend/src/config. "oss" is the
# default (Mike). "libris-colleague" is the Attune Legal product name.
# "mode-law" is a placeholder. Prefixed NEXT_PUBLIC_ because branding is
# client-visible. See config/README.md.
# NEXT_PUBLIC_DEPLOYMENT_PROFILE=oss
